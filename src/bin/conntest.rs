extern crate uosql;
extern crate byteorder;
extern crate rustc_serialize;
extern crate bincode;
/*
use uosql::net;
use bincode::rustc_serialize::{encode_into};
use bincode::SizeLimit;
*/
/// test of some functions and structs by net
fn main() {
    /*
    //test if the commands are correctly decoded
    //use std::io::Cursor;        // stream to read from
    let mut vec = Vec::new();   // stream to write into

    //vec.push(net::Cnv::CommandPkg as u8);
    let command_encode = encode_into(&net::Command::Query("select".into()), &mut vec, SizeLimit::Bounded(1024));

    println!("Commands-> {:?}", vec);  
    */
}
